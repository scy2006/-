#ifndef ALGORITHM_H
#define ALGORITHM_H

#include "common.h"

int algorithm(int** arr, int* height, int* best_angle, int* best_position,
    char shape, char next, int* time, double* max, int ans);

#endif